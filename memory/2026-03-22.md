# 2026-03-22

## OpenClaw 配置变更

- **模型切换**: 从 MiniMax M2.7 切换到 Xiaomi MiMo-V2-Pro 作为 primary model
- **小米 API Key**: `sk-c39z068xcx8a85mnkbw97v3prrkwsu2r45mq3v2mhg3lgj4x`
- **小米 API Endpoint**: `https://api.xiaomimimo.com/anthropic`
- 三个 MiMo V2 版本: Pro (旗舰), Omni (全模态), Flash (轻量快速), TTS (语音合成)
- 用户在 3/19 询问了 dashboard 配对问题，目前状态是 `--auth none` 仍然要求配对，未解决

## ClawHub Skills

- 尝试安装 `self-improving-agent` 和 `ws-agent-browser`，全部失败
- 原因: ClawHub API 对当前出口 IP (91.243.81.34, 欧洲) 限速极严
- ClawhHub 的 install 端点 limit 为 20 次/窗口，且恢复很慢
- 用户让我用 `clawdhub install` 命令，但 IP 被限速
- ClawHub 搜索功能 (search/explore/inspect) 正常，install 被限

## ClawHub Skills 安装（续）

- 用户多次要求安装 `self-improving-agent` 和 `agent-browser` (即 ws-agent-browser)
- 尝试 `npx clawhub install` 和 `npx clawdhub install` 均失败，Rate limit exceeded
- 建议用户本地终端运行安装命令

## 其他

- 用户查了 18790 端口，未被占用
- 模型后来切回了 MiniMax-M2.7（用户手动或自动回退）
- 用户问过我当前是什么大模型（回答：小米 MiMo-V2-Pro）

## 未解决问题

- OpenClaw dashboard "pairing required" 配对问题未解决
- ClawHub skills 安装因 IP 限速失败，需要用户本地终端安装
- 用户问过如何卸载 OpenClaw (docs.openclaw.ai/install/uninstall)
